version: "3"

services:
  web:
    image: web
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: posto
    volumes:
      - ".:/usr/src/app"
    ports:
      - 8001:8001
    environment:
      - FLASK_DEBUG=1
      - PYTHONUNBUFFERED=0
